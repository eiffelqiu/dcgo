# Docker Compose Template

docker static website && docker golang service

## Run

	$ docker-compose up

	access http://localhost:8080

## Shutdown

	$ docker-compose down

## Clean docker images

	$ ./clean.sh